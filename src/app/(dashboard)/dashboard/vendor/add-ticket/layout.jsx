export const metadata = {
    title: "Add New Ticket | Vendor Dashboard - Ticketix",
    description:
        "Create and publish new event tickets, set pricing, manage availability, and start selling through Ticketix vendor dashboard.",
};

const AddTicketsLayout = ({ children }) => {
    return <>{children}</>;
};

export default AddTicketsLayout;
