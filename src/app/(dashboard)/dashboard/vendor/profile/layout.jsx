export const metadata = {
    title: "Vendor Dashboard | Ticketix",
    description:
        "Manage your event listings, track real-time sales, and analyze performance as a registered vendor on Ticketix.",
};

const VendorLayout = ({ children }) => {
    return <>{children}</>;
};

export default VendorLayout;
