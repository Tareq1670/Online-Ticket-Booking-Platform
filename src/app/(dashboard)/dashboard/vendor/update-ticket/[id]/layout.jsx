export const metadata = {
    title: "Update Ticket | Vendor Dashboard - Ticketix",
    description:
        "Update your ticket details, pricing, availability, and event information from your Ticketix vendor dashboard.",
};

const UpdateTicketLayout = ({ children }) => {
    return <>{children}</>;
};

export default UpdateTicketLayout;
