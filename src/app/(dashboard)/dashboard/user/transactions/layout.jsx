export const metadata = {
    title: "Transaction History | Ticketix",
    description:
        "View and track all your payment history, transaction details, and billing information on Ticketix.",
};

const TransactionLayout = ({ children }) => {
    return <>{children}</>;
};

export default TransactionLayout;
