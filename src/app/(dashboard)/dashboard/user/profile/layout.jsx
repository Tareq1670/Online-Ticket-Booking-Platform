export const metadata = {
    title: "My Profile | Ticketix",
    description:
        "Manage your Ticketix account profile, update your personal information, and view your account activity.",
};

const ProfileLayout = ({ children }) => {
    return <>{children}</>;
};

export default ProfileLayout;
