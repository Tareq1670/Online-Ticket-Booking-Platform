export const metadata = {
    title: "Manage Tickets | Admin Dashboard - Ticketix",
    description:
        "View and manage all tickets, track availability, monitor sales, and update ticket information on Ticketix.",
};

const ManageTicketsLayout = ({ children }) => {
    return <>{children}</>;
};

export default ManageTicketsLayout;
