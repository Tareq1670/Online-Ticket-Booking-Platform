export const metadata = {
    title: "Admin Profile | Ticketix",
    description:
        "Manage your admin profile, account information, and profile settings on Ticketix.",
};

const AdminProfileLayout = ({ children }) => {
    return <>{children}</>;
};

export default AdminProfileLayout;
